{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
            <div>
                <h4>Manage your invitations</h4>
                The search function was not implemented... yet!
            </div>
            <div class="invitation-index">
                <h4>Invitations received</h4>
                {{ include('invitation/table.html.twig', { 'invitations' : invitationsReceived, 'action' : 'decline' }) }}
            </div>
            <div class="invitation-index">
                <h4>Invitations sent</h4>
                {{ include('invitation/table.html.twig', { 'invitations' : invitationsSent, 'action' : 'cancel',  }) }}
            </div>
        </div>
    </div>
{% endblock %}